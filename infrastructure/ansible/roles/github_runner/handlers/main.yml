---
# No handlers. Verification runs inline in tasks/main.yml because the role now
# manages a list of runners and each one must be checked by its own container
# name — a notified handler cannot see the loop variable that names it.
