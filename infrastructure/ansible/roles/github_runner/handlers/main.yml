---
- name: Verify runner container
  community.docker.docker_compose_v2:
    project_src: /opt/github-runner
    state: present
  register: runner_status

- name: Show runner status
  ansible.builtin.debug:
    msg: "Runner container state: {{ runner_status.services.runner.State.Status | default('unknown') }}"
