---
- name: Provision VPS
  hosts: vps
  become: true
  gather_facts: true

  roles:
    - common          # system updates, UFW, SSH hardening
    - docker          # Docker Engine + Compose plugin
    - github_runner   # runner as Docker container via myoung34/github-runner
    - kubernetes      # kind, k3d, kubectl, helm for training
